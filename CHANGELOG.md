# История версий

Формат: адаптированный [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/)
+ семантическое версионирование МАЖОР.МИНОР.ПАТЧ:

- **МАЖОР** — несовместимые изменения, требующие ручных действий сверх
  обычного редеплоя (например, миграция БД, которую нельзя откатить, или
  смена формата конфига).
- **МИНОР** — новая функциональность, не ломающая существующее поведение.
- **ПАТЧ** — багфиксы, не меняющие поведение API/БД.

Номер версии лежит в файле `VERSION` в корне репозитория и копируется в
Docker-образ (см. `Dockerfile`) — отображается в дашборде на вкладке
"Логи" (панель "Системные показатели", `GET /system/metrics`). Реальный
мотив: без него было невозможно быстро отличить "новый код уже
развернулся" от "docker собрал из кеша старый слой" при отладке редеплоя.

История ДО этого файла отдельно не велась — она доступна через `git log`.
Версия `1.0.0` ниже фиксирует состояние прода на момент введения файла, а
не единственный релиз одного дня.

## [1.5.1] - 2026-09-24

### Fixed
- **После рестарта доля открытых позиций в риск-лимитах считалась
  нулевой.** Найдено при плановом мониторинге (прод): у всех позиций,
  восстановленных после рестарта, `risk_state.open_positions` показывал
  0.0. `_sync_open_positions_from_execution_engine` брал баланс для
  расчёта доли только в paper-режиме, а в real-режиме — `None`. Итог:
  суммарная нагрузка (`total_notional_pct`) после каждого рестарта
  (включая каждый редеплой) была занижена, и лимит
  `max_total_notional_pct` не учитывал уже открытые позиции. Теперь доля
  считается от баланса биржи — того же источника, что и при открытии
  позиции; если баланс получить не удалось, это пишется в лог.

## [1.5.0] - 2026-09-24

### Added
- **Графики в развороте открытых позиций и закрытых сделок в стиле
  терминала биржи** (по запросу пользователя):
  - переключатель таймфреймов 1м / 5м / 15м / 1ч / 4ч / 1Д; по умолчанию —
    самый мелкий, при котором вся сделка умещается примерно в 300 свечей
    (`GET /chart/candles` принимает `timeframe`);
  - объём торгов отдельной гистограммой и скользящие средние MA7 / MA25 /
    MA99 — включаются и выключаются кнопками;
  - OHLC-легенда под курсором: O/H/L/C, изменение свечи в %, объём,
    значения MA;
  - зона прибыли/убытка — заливка между ценой и уровнем входа на время
    жизни позиции (зелёная в плюсе, красная в минусе, с учётом шорта);
  - полоса над графиком: сторона и плечо, вход, текущая цена, движение
    цены, ROE с учётом плеча, PnL в USDT, расстояние до SL; у закрытой
    сделки — средняя цена выхода, PnL, длительность и лучшая цена после
    выхода (линия на графике — из анализа исходов сделок);
  - маркеры входа (стрелка по стороне сделки) и каждого выхода с PnL ноги;
    достигнутые TP показаны пунктиром с отметкой ✓;
  - живое обновление последней свечи открытой позиции раз в 10 с (только
    пока график виден);
  - полноэкранный режим (Esc — выход), кнопка «показать сделку целиком».
- Колонка графика в развороте шире колонки с источником сигнала (3:2).

### Fixed
- Фон развёрнутых строк таблиц был жёстко затемнён под тёмную тему и в
  светлой выглядел серым.

## [1.4.3] - 2026-09-24

### Fixed
- **Плечо в скобках после пары не распознавалось.** Реальный инцидент
  (прод, Crypto Pirates Channel): "МОНЕТА: $POL/USDT (2-5x)" —
  `parsed_leverage` оказался пустым, и позиция открылась с дефолтным
  плечом 1x. Regex-парсер знал только плечо после ключевого слова
  ("Плечо:", "Leverage") или сразу после LONG/SHORT. Теперь
  распознаются "(2-5x)", "(10x)", "(x20)", "(x2-x5)"; для диапазона
  берётся нижняя граница — как и для "Плечо: 25-30х". Множитель x/х
  внутри скобок обязателен, поэтому "(5%)", "(1)", "(2-5)" не
  принимаются за плечо; явное "Плечо: N" по-прежнему в приоритете.

## [1.4.2] - 2026-09-23

### Fixed
- **Найдено при плановом мониторинге**: закрытие позиции вне цикла бота
  терялось без PnL, если у позиции до этого были частичные TP. Реальный
  инцидент (прод, RENDER/USDT, открыта 20.09, 3 частичных TP): позиция
  закрылась биржевым SL, путь по id SL-ордера сделок не нашёл, а фолбэк
  `_finalize_via_recent_trade_history` суммировал с финальной продажей и
  продажи частичных TP, которые бот уже записал сам (1715.4 против
  отслеживаемых 1143.7) -> "слишком расходятся", позиция снята без PnL,
  а открывающий ордер помечен rejected. Теперь уже записанные закрытия
  этой позиции исключаются по id биржевых ордеров/сделок из БД, и окно
  поиска начинается с последнего из них, а не с момента открытия.

## [1.4.1] - 2026-09-23

### Fixed (производительность)
- **Лишняя нагрузка на API биржи от дашборда.** `/status` делал
  `fetch_balance`, а `/balances` — `fetch_balance` плюс `fetch_ticker` на
  КАЖДУЮ валюту при каждом опросе (каждые 5с, из каждой открытой вкладки,
  даже при свёрнутой секции балансов) — лишние запросы конкурировали с
  торговыми за rate-limit Bybit (в логах регулярно "Too many visits" при
  загрузке свечей). Теперь короткий TTL-кэш (10с/15с) с одним общим
  запросом на всех одновременных клиентов, сбрасывается при смене
  режима/рынка/биржи и после "Продать все остатки"; свёрнутая секция
  балансов опрашивается раз в минуту. На проде `/balances` отвечал 4.6с.
- **`GET /telegram/channels`** (опрос каждые 5с) загружал ВСЕ сигналы
  каждого канала вместе с текстами сообщений ради `len()` — теперь один
  `COUNT … GROUP BY`. Прод: ~1.0с на 8 каналов / 436 сигналов.
- **`GET /telegram/channels/stats`** — два агрегирующих запроса на все
  каналы вместо двух запросов на каждый канал (N+1) с полными строками.
- **Торговый цикл**: индикаторы по каждому символу считались дважды за
  итерацию (второй раз — при записи обучающего примера ML из сырых
  свечей). Теперь переиспользуются уже посчитанные.

## [1.4.0] - 2026-09-23

### Added
- **Редизайн дашборда**: новая дизайн-система (токены, шрифты Inter /
  JetBrains Mono, цифры одинаковой ширины в таблицах — колонки не
  «прыгают» при обновлении), контрастность подписей повышена до WCAG AA.
- **Светлая тема** (кнопка 🌓 в топбаре, выбор запоминается), графики
  перекрашиваются вместе с темой.
- **График «Динамика баланса»** на вкладке «Дашборд» по почасовым снимкам
  (24ч / 7д / 30д / всё) + изменение, макс. просадка за период,
  нереализованный PnL, win rate, Sharpe. `GET /performance` принимает
  `hours` и `limit` (до 5000).
- **KPI-карточки с индикаторами риск-лимитов**: использование дневного
  лимита убытка, слотов позиций и максимальной просадки.
- **Палитра команд** (Ctrl/⌘+K): переход по вкладкам, обновление, тема,
  быстрый переход к открытой позиции по символу. Горячие клавиши 1–5 —
  вкладки, R — обновить.
- **Карточки ML-моделей** на вкладке «Обучение»: skill относительно
  базовой линии, log-loss, balanced accuracy, размеры выборок, статус
  активная/теневая и причина решения champion/challenger (см. 1.3.0).
- Бейдж **DEMO** рядом с REAL, когда бот торгует на демо-счёте биржи
  (`/status` отдаёт `use_exchange_sandbox`).
- Топбар прилипает к верху страницы при прокрутке.

### Changed
- Обновление дашборда: один запрос за раз (вызовы от таймера, WebSocket и
  кнопок больше не накладываются), независимые секции грузятся
  параллельно и только для видимой вкладки, опрос приостанавливается,
  пока вкладка браузера свёрнута. Раньше ~9 эндпоинтов опрашивались
  последовательно каждые 5с на любой вкладке.

### Fixed
- Новый тост гас раньше времени по таймеру предыдущего.

## [1.3.0] - 2026-09-23

### Fixed
- **ML: перепутаны P(роста) и P(падения).** `MLInference.predict_direction`
  брал `proba[1]`/`proba[-1]` по позиции в массиве, а классы модели —
  `[-1, 0, 1]`: "P(up)" на самом деле был P(нейтрально), "P(down)" — P(роста).
  Теперь вероятности сопоставляются по `model.classes_`. (В проде сейчас
  активен режим «сигналы», алго-стратегии не торговали — но прогноз в
  подробностях позиции на дашборде показывал перевёрнутые значения.)
- **ML: утечка будущего при валидации.** Случайный `train_test_split` на
  автокоррелированных часовых свечах завышал метрики. Теперь разбиение
  train → val → test строго по времени с зазором в горизонт метки (5ч).
- **ML: расхождение признаков train/serve.** `_build_strategy_data` не
  передавал в модель 11 признаков, на которых она обучалась (`roc_*`,
  `return_10/20`, `realized_vol_60`, `close_position`, ...) — на инференсе
  вместо них подставлялся 0. Теперь передаются все `ML_FEATURE_COLS`,
  а отсутствующий признак — NaN (LightGBM обрабатывает пропуски нативно).
- **ML: активация модели из API не доходила до инференса.** У `MLInference`
  был свой отдельный реестр и кэш — ручная активация версии не влияла на
  предсказания до рестарта. Теперь реестр общий, активация сбрасывает кэш.
- Обученный `signal_quality_classifier` нигде не использовался.

### Added
- **Champion/challenger:** новая версия модели активируется, только если
  она лучше текущей активной на свежих отложенных данных (после даты
  обучения текущей); иначе сохраняется как shadow. В метриках —
  `log_loss`, `balanced_accuracy`, `f1_macro`, базовая линия и `skill`.
- Модель без преимущества над базовой линией (`skill <= 0`) не влияет на
  торговлю.
- Модель качества сигнала участвует в оценке Telegram-сигналов: сдвиг
  quality на `ml_signal_quality_weight * (базовая доля убыточных -
  P(убыток))` — в среднем ~0, пороги каналов не смещаются. Включается
  только после переобучения по новому пайплайну (у старых моделей нет
  `skill`). Модель переобучается по расписанию вместе с остальными.
- Win-rate канала в скоринге — с байесовским сглаживанием Beta(2, 2)
  (канал с одной выигрышной сделкой больше не считается идеальным).
- Удаление с диска старых неактивных версий моделей (`ml_keep_model_files`).

### Changed
- Обучение (Optuna + LightGBM) выполняется в отдельном потоке, не
  блокируя торговый цикл и веб-панель. Optuna оптимизирует log-loss/MAE.
- Признаки с абсолютным масштабом цены (`obv`, `atr_14`, `macd`) исключены
  из обучения — модель общая для всех символов.
- `restore_channel_stats_from_db` — один JOIN-запрос вместо N+1.

## [1.2.5] - 2026-09-22

### Fixed
- **Найдено при плановом мониторинге**: `_auto_adopt_untracked_futures_
  position` доверял снимку из bulk `fetch_positions()` без перепроверки.
  Реальный инцидент (прод, APT/USDT): позиция реально закрылась в 01:02
  (бот сам обнаружил и зафиксировал закрытие по сработавшему биржевому
  SL), но в 12:47 — 11+ часов спустя — bulk `fetch_positions()` всё ещё
  вернул для неё "призрачную" строку с ненулевым `contracts` и УСТАРЕВШИМ
  `entryPrice`. Позиция была по новой "подхвачена" с тем же самым
  SL-триггером, что и 11 часов назад — к текущей цене он уже оказался по
  невалидную сторону ("expect Falling, but trigger_price >= current"),
  биржа отклонила установку SL, и через минуту сверка обнаружила, что
  подхватывать было уже нечего. Сестринский метод `adopt_unconfirmed_
  futures_position` уже подстраховывался точно так же — перепроверял
  позицию через ТОЧЕЧНЫЙ `fetch_position()` перед тем как ей довериться,
  а не брал снимок bulk-листинга как есть; в `_auto_adopt_untracked_
  futures_position` такой перепроверки не было. Теперь перед подхватом
  выполняется точечный `fetch_position()` — если он показывает
  `contracts=0`, авто-подхват тихо отменяется вместо создания фантомной
  позиции с устаревшим SL-триггером.

## [1.2.4] - 2026-09-22

### Fixed
- Найдено при плановом мониторинге: `@signalyp` прислал "#ARB, забрал
  первый тейк и вышел в безубыток, закрыл 50% позиции" — не распознано
  ни одним парсером, хотя `_PARTIAL_UPDATE_PATTERN` ("забрал ... тейк")
  уже матчился. Причина: `is_position_update_report` требовала ЕЩЁ и
  упоминание "депозит" — канал в этот раз его не написал. Условие
  убрано: конструкция "забрал(и) ... тейк/цель" (слово "забрал(и)" сразу
  перед номером целевого уровня) сама по себе уже достаточно специфична
  — в настоящем сигнале "тейк"/"цель" встречается только со списком
  целевых цен, без слова "забрал(и)" перед ними; упоминание депозита
  было лишним дополнительным условием, а не необходимым для отличия от
  реального сигнала.

## [1.2.3] - 2026-09-21

### Fixed
- Найдено при плановом мониторинге: `@Treyding_Signaly_Kripto` прислал
  "#W/USDT - Длинная🟢" — не распознано ни одним парсером. Причина:
  тикер "W" (Wormhole, реальная однобуквенная монета на Bybit) короче
  минимума в 2 буквы (`[A-Z]{2,10}`) в обоих паттернах извлечения пары
  (явный `/USDT` и хэштег без quote-валюты). Оба паттерна уже надёжно
  заякорены (явная quote-валюта после "/", либо "#" перед тикером),
  поэтому снижение минимума до 1 буквы безопасно — не рискует принять
  случайное одиночное слово текста за тикер.

## [1.2.2] - 2026-09-20

### Fixed
- **Найдено при плановом мониторинге, критично**: `execution_engine.
  can_execute()` (общая точка входа ВСЕХ ордеров — ручных/алго/Telegram,
  проверяется внутри `create_order()`) проверял только `kill_switch_
  active` и `paused`, но НЕ `daily_loss_limit_reached`. Алго-путь
  (`risk_manager.check_signal` → `can_trade()`) дневной лимит убытков уже
  проверял, но Telegram-путь с `auto_execute` сознательно обходит
  `check_signal` (см. комментарий в `_on_telegram_signal` про доливку/
  already-open-position) и полагался на то, что "kill switch и пауза
  всё равно останавливают" — предполагая, что дневной лимит убытков
  входит в тот же общий стоп, хотя он был отдельным флагом. Реальный
  инцидент (прод): дневной лимит убытков (-50 USD) был достигнут около
  20:xx UTC, а в 22:14 UTC Telegram-сигнал (@Treyding_Signaly_Kripto)
  всё равно открыл НОВУЮ позицию (ONT/USDT). `can_execute()` теперь
  дополнительно проверяет `daily_loss_limit_reached` — закрывает разрыв
  для ВСЕХ путей исполнения ордеров разом, а не только для Telegram.

## [1.2.1] - 2026-09-20

### Fixed
- Обнаружено при визуальной проверке через скриншот прода: панель
  "Анализ исходов сделок" (1.2.0) наследовала `.metric-grid` от узкой
  боковой панели "Системные показатели" (`grid-template-columns: repeat(
  auto-fill, minmax(110px, 1fr))`) — в широкой секции основного контента
  auto-fill создавал больше колонок, чем есть карточек, оставляя заметный
  пустой прямоугольник справа. Новый модификатор `.metric-grid-wide`
  (`auto-fit` вместо `auto-fill`) схлопывает лишние колонки.

## [1.2.0] - 2026-09-20

### Added
- Панель "Анализ исходов сделок" на дашборде (вкладка "Дашборд", под
  таблицей "Последние сделки") — витрина для `GET /analytics/trade-
  outcomes` (добавлен в 1.1.0 вместе с самим отслеживанием цены после
  закрытия сделки, см. запись 1.1.0 ниже): счётчики по вердиктам
  (оптимально/SL слишком тесный/TP слишком консервативный/ранний выход) и
  таблица сделок с наибольшей упущенной прибылью (символ, вердикт, цена
  закрытия/лучшая достигнутая, факт./оптимальный PnL%).

## [1.1.0] - 2026-09-20

### Added
- По запросу пользователя: отслеживание движения цены ПОСЛЕ закрытия
  сделки, чтобы понять постфактум, как нужно было бы выставить вход/SL/TP
  для максимальной прибыли. Новая таблица `trade_outcome_tracking`
  (миграция `020`) и периодическая задача (`outcome_tracking_check_
  interval_minutes`, по умолчанию 30 мин, см. `src/execution/trade_
  outcome_tracker.py`): для каждой ПОЛНОСТЬЮ закрытой позиции отслеживает
  лучшую достигнутую после закрытия цену в пользу исходного направления
  сделки, пока не найдёт локальный экстремум (откат от пика на
  `outcome_tracking_retracement_pct`, по умолчанию 30%, от размера уже
  пройденного движения — ограничено сверху `outcome_tracking_max_days`,
  по умолчанию 14 дней, на случай, если цена вообще не разворачивается).
  По результату классифицирует сделку: `sl_too_tight` (закрылись в минус,
  цена потом ушла в плюс от входа), `tp_too_conservative` (закрылись в
  плюс, но цена ушла заметно дальше), `optimal`/`premature_exit`.
  Доступно через `GET /trades/{id}/detail` (поле `outcome_tracking`) и
  новый сводный `GET /analytics/trade-outcomes`.
- "Обучение LLM" на этих данных (по формулировке запроса пользователя, с
  учётом того, что LLM в проекте — только парсер текста сигналов, без
  пайплайна дообучения): LLM-фолбэки парсера сигналов (`llm_parser.py`,
  `gemini_parser.py`, `groq_parser.py`, `cerebras_parser.py`) теперь
  обогащают системный промпт кратким контекстом по фактическим исходам
  СДЕЛОК канала ("N прошлых сделок канала закрылись по SL, а цена потом
  развернулась в прибыль" и т.п., см. `channel_outcome_context.py`) —
  строго информационно, только для confidence/is_signal при неоднозначном
  сообщении. Явно оговорено промптом, что этот контекст НЕЛЬЗЯ
  использовать, чтобы менять или придумывать явно указанные в тексте
  цены — это нарушило бы базовый принцип парсера "never invent, guess, or
  extrapolate prices". Выключается `llm_channel_outcome_context_enabled`.

## [1.0.8] - 2026-09-20

### Fixed
- Найдено при плановом мониторинге: `orders.order_id_exchange` объявлена
  `VARCHAR(100)`, но `_record_external_close` (закрытие позиции,
  обнаруженное вне цикла бота) пишет туда ID ВСЕХ отдельных сделок
  закрытия через запятую. Реальный инцидент (прод): позиция закрылась
  тремя отдельными частичными сделками — 3 UUID биржи по 36 символов + 2
  запятые = 110 символов, что превысило лимит и уронило INSERT ордера с
  `StringDataRightTruncationError`. Позиция при этом уже была удалена из
  памяти бота (`self.real_positions.pop`) ДО попытки записи — при падении
  записи закрытие терялось из истории БЕЗ следа, и `risk_manager.
  on_trade_closed`/`on_position_closed` для этой сделки не вызывались
  (PnL сделки не попадал в дневной риск-стейт). Колонка расширена до
  `TEXT` (миграция `019_widen_order_id_exchange`, безопасный `ALTER
  COLUMN TYPE`, без потери данных).

## [1.0.7] - 2026-09-20

### Fixed
- Найдено при плановом мониторинге: `@kripto_signaly3`/`@kripto_signalyX`
  прислали "▪️ **INJ**, забрали все цели, зафиксировал сделку полностью.
  \n\nприбыль: 61.13$\nактуальный депозит: 387.84$" — та же суть, что и
  раньше распознанные отчёты о закрытии, но `is_position_update_report`
  всё равно не сработал. Причина: паттерн `цель\w*` требует буквально
  "цель" (с мягким знаком) как префикс, а во множественном числе/
  косвенных падежах существительного "цель" мягкий знак пропадает
  ("цели", "целей", "целям" — 3-е склонение), т.е. "цели" НЕ начинается с
  "цель" как строка. `_PARTIAL_UPDATE_PATTERN` исправлен на
  `цел[ьи]\w*`, покрывает и единственное ("цель"/"целью"), и
  множественное число ("цели").

## [1.0.6] - 2026-09-20

### Fixed
- Найдено при плановом мониторинге: `risk_manager.state.daily_loss_limit_
  reached` мог оставаться `True` (блокируя ЛЮБОЙ новый вход) существенно
  дольше одного календарного дня, как задумано — `check_daily_loss_limit_
  reset()` вызывался ТОЛЬКО реактивно, из `on_trade_closed()` при закрытии
  сделки. Если после срабатывания лимита ни одна позиция не закрывалась
  (реальный случай на проде — все открытые позиции уже были плоскими),
  сброс не происходил вообще, даже после наступления новой полуночи.
  Теперь `check_daily_loss_limit_reset()` дополнительно вызывается на
  каждой итерации основного цикла (`_trading_iteration`), независимо от
  закрытия сделок — метод идемпотентен и безопасен для частого вызова.

## [1.0.5] - 2026-09-19

### Fixed
- Найдено при плановой проверке /logs после деплоя 1.0.4: `@signaly_
  treyding`/`@kripto_signaly7` шлют отчёт о закрытии позиции словом
  "цель", а не "тейк" ("✅Забрали 3ю цель по вчерашнему сигналу OP,
  закрыл 100% позиции!\n\nДепозит марафона: 20611.6$") — `is_position_
  update_report` понимал только "забрали ... тейк", это не матчилось.
  `_PARTIAL_UPDATE_PATTERN` расширен на альтернативу "цель".

## [1.0.4] - 2026-09-19

### Fixed
- Найдено при плановой проверке /logs после деплоя 1.0.3 (спам по
  предыдущим форматам подтверждённо прекратился): `@Treyding_Signaly_
  Kripto` раз в сутки шлёт агрегированную сводку прибыли по десятку пар
  сразу ("📈 Прибыль VIP-канала за последние 24 часа\n\n```\nLTCUSDT :
  +41.01% 🟢\n...") — не сигнал и не отчёт по одной сделке, структурно не
  может дать парсерам одну пару/цену. Новый `is_daily_summary_report`
  распознаёт этот формат до любого парсера, той же схемой, что и
  `is_closed_trade_report`/`is_locked_teaser`/`is_position_update_report`.

## [1.0.3] - 2026-09-18

### Fixed
- Найдено при плановой проверке /logs после деплоя 1.0.2 (спам от
  `@Treyding_Signaly_Kripto` подтверждённо прекратился): каналы
  `@kripto_signaly3`/`@kripto_signalyX` шлют текстовое обновление статуса
  уже открытой позиции ("забрали второй тейк, зафиксировал 50% и стоп
  ставлю в бу... прибыль: 9.63$... актуальный депозит: 321.51$") — та же
  суть, что у `is_closed_trade_report`, но прибыль в ДОЛЛАРАХ, а не в
  процентах, и без отметки "цель N ✅", поэтому её маркеры не срабатывали.
  Новый `is_position_update_report` распознаёт этот формат до любого
  парсера — не тратит LLM-запрос и не логируется как сбой.

## [1.0.2] - 2026-09-18

### Fixed
- `_handler()` логировал WARNING "не распознано ни одним парсером" на
  КАЖДОМ сообщении, где `parse_telegram_signal()` вернул `None` — не
  различая "не сигнал по определению" (уже распознанный отчёт о закрытой
  сделке) и "настоящий сбой разбора". Расширение `is_closed_trade_report`
  в 1.0.1 поэтому не остановило спам в логе. Добавлен также
  `is_locked_teaser` — тизер без единого реального числа ("Направление
  позиции: 🔐 ... Детали сигнала в VIP") тоже больше не тратит LLM-запрос
  и не логируется как сбой.
- Явные правила канала после TP1 (доли/SL-правило, версия 1.0.1) и другие
  фиксы регулярно требуют редеплоя — с частыми рестартами процесса
  всплыла реальная жалоба пользователя: бот переставал РЕДАКТИРОВАТЬ
  исходное сообщение об открытии при частичных закрытиях уже открытых на
  момент рестарта Telegram-позиций и начинал отвечать новыми сообщениями.
  Причина — `_notif_header`/`_notif_sl_value`/`_notif_tp_values` нигде не
  персистились. Теперь восстанавливаются при рестарте из
  `TelegramSignal`/`Order` (та же связь, что и у `take_profits`/
  `tp_weights`), пока уведомление об открытии вообще было отправлено.

## [1.0.1] - 2026-09-18

### Fixed
- `@Treyding_Signaly_Kripto` регулярно шлёт отчёты о статусе уже открытой
  сделки (не новые сигналы), которые не распознавались ни одной из двух
  pre-parse проверок (`is_closed_trade_report`/`extract_stop_hit_pair`) и
  спамили лог WARNING'ами "не распознано ни одним парсером":
  - "Цель достигнута N ✅" (лишнее слово между ключевым словом и номером
    цели, не матчилось `_HIT_TARGET_PATTERN`);
  - "All targets achieved 😎" (сводка без номера цели и с другим эмодзи);
  - "Позиция была остановлена ⛔" — русский вариант отчёта о срабатывании
    СВОЕГО стопа канала (`_STOP_HIT_PATTERN` понимал только английское
    "Stop Target Hit"); без распознавания эта наша позиция закрывалась не
    немедленно по отчёту канала, а только на следующей плановой сверке.

## [1.0.0] - 2026-09-18

### Added
- Панель "Системные показатели" (CPU/память/диск/load average/аптайм
  процесса) на вкладке "Логи" дашборда.
- Диагностический эндпоинт `GET /telegram/channels/{id}/unparsed-samples`
  (полный текст последних нераспознанных сообщений канала).
- Отображение маржи и плеча отдельной строкой в подробностях открытой
  позиции.
- Явные правила канала — доли частичного закрытия по каждому TP-уровню и
  правило переноса SL после TP1 ("в безубыток" вместо дефолтного "на
  полпути к входу") — распознаются из свободного текста сигнала
  (`extract_tp_weights`/`extract_post_tp1_sl_rule`) и переопределяют
  дефолт бота для конкретной сделки. Новые колонки `telegram_signals.
  parsed_tp_weights`/`parsed_post_tp1_sl_rule` (миграция
  `018_telegram_signal_tp_weights`).
- Этот файл (`CHANGELOG.md`) и файл `VERSION`.

### Changed
- SL после первого частичного TP переносится не в безубыток, а на
  полпути от исходного SL к цене входа (`halfway_to_entry_stop_price`) —
  по явному запросу пользователя. Канал может явно переопределить это
  правило текстом сигнала (см. выше).
- Часть сообщений в логах переведена на русский; лог загрузки свечей по
  символу понижен с INFO до DEBUG (не спамил вкладку "Логи" на каждом
  цикле по каждому активному символу).

### Fixed
- `_handler()` (`channel_monitor.py`) больше не теряет бесследно
  сообщения, которые не смог разобрать ни один парсер (regex + все
  LLM-фолбэки) — пишет WARNING в лог вместо молчания.
- Regex-парсер сигналов теперь распознаёт:
  - кириллические слова стороны сделки ("Короткая"/"Длинная") и цены,
    обёрнутые в Markdown-бэктики (`@Treyding_Signaly_Kripto`);
  - голое "ВХОД:" (без слова "точка"/"цена" перед ним) и список целей,
    разделённый тире/en-dash (`@topslivs`);
  - строчные "long"/"short" рядом с голым тикером без хэштега и
    quote-валюты (`@kripto_signalyX`, `@kripto_signaly3`);
  - хэштег тикера, разбитый Markdown-звёздочками (`#**APT**`), и
    латинскую букву "C" вместо кириллической "С" в слове "Cтоп"
    (`@signalyp`).
- В Docker-образ бота добавлен `curl` — без него healthcheck в
  `docker-compose.yml` всегда падал с "command not found" независимо от
  реального состояния приложения.
